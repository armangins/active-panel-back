# Server Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database
# For local development, use MongoDB locally or MongoDB Atlas
# For Render, this will be automatically set from the MongoDB database service
MONGODB_URI=mongodb://localhost:27017/active-panel

# Session Security
# Generate a strong random string for production (e.g., use: openssl rand -base64 32)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Encryption Key (for encrypting WooCommerce credentials)
# Must be at least 32 characters long
# Generate a strong random string for production (e.g., use: openssl rand -base64 32)
# WARNING: Changing this key will make all encrypted data unreadable!
ENCRYPTION_KEY=your-32-character-encryption-key-change-this

# Google OAuth (Optional - for Google login)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
